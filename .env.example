# Optional: upgrade routing from OSRM to Google live traffic (server-side only)
GOOGLE_MAPS_API_KEY=
GOOGLE_DISTANCE_MATRIX_API_KEY=

# Optional: render routes on Google Maps in Plan Route + Logistics Dashboard
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# GCP setup: enable Directions, Geocoding, Routes, Distance Matrix, Maps JavaScript, Places
# https://console.cloud.google.com/google/maps-apis/credentials
